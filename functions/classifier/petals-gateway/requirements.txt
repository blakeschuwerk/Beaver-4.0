# Pinned to a known-working set on macOS arm64 (Python 3.11). Petals/hivemind
# haven't been updated since ~2023 and break on current torch/transformers/
# accelerate releases (hivemind needs a removed torch.cuda.amp internal;
# transformers 4.34 needs an old huggingface-hub; newer accelerate needs a
# huggingface-hub function this old huggingface-hub doesn't have). Don't
# casually bump these without re-testing the full import chain.
torch==2.1.2
petals==2.2.0.post1
hivemind==1.1.10.post2
transformers==4.34.1
huggingface-hub==0.17.3
accelerate==0.24.1
fastapi==0.125.0
uvicorn[standard]==0.49.0
pydantic==1.10.26
