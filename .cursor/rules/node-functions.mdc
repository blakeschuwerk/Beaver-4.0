---
description: Node.js Cloud Run function conventions for Beaver 4.0
globs: functions/dispatcher/**,functions/classifier/**,functions/personalization/**,packages/shared/**
alwaysApply: false
---

# Node Function Conventions

## Structure

- `src/index.ts` — Express HTTP handler (Pub/Sub push endpoint at `POST /`).
- `src/{name}.ts` — Business logic, exported for testing.
- Dockerfile builds from monorepo root (needs `packages/shared`).

## Patterns

```typescript
// Parse Pub/Sub envelope or direct JSON body
if (envelope?.message?.data) {
  const decoded = Buffer.from(envelope.message.data, 'base64').toString('utf-8');
  message = schema.parse(JSON.parse(decoded));
}

// Mock mode for local dev
const MOCK_MODE = process.env.MOCK_MODE === 'true';
```

## Dependencies

- Import contracts from `@beaver/shared`.
- Use workspace protocol: `"@beaver/shared": "workspace:*"`.
- GCP clients: `@google-cloud/firestore`, `@google-cloud/bigquery`, `@google-cloud/pubsub`, `@google-cloud/storage`.

## Health check

Every service exposes `GET /health` returning `{ status: 'ok', service: 'beaver-{name}' }`.

## Failure & observability

- **No silent fallbacks in production.** Mock/heuristic paths run only when the explicit
  flag is set (`LLM_MOCK_MODE=true` / `MOCK_MODE=true`). With the flag off, a failed
  dependency must throw (e.g. `LlmUnavailableError` from `@beaver/shared`) so the handler
  returns non-2xx and the Pub/Sub message dead-letters for retry — never write fake data.
- **Log outcomes structurally** with `logEvent()` from `@beaver/shared` (status, latency,
  attempt). Read with `pnpm logs:errors`. Full rationale: CLAUDE.md "Failure &
  Observability Principles".
